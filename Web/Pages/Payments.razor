@page "/"
@page "/payments"
@page "/payments/{Month}/{Year}"


Displaying Payments for current month/year
<MonthYearSelector ReturnURL="/payments" />

<div class="rounded" >
    <table class="table table-sm">
        <thead>
            <tr>
                <td></td>
                <td hidden>Id</td>
                <td>Date</td>
                <td>Note</td>
                <td></td>
            </tr>
        </thead>

        <tbody>
                @foreach(var item in PaymentItems)
                {
                    <tr>
                        <td><div class="mouse-pointer" @onclick="() => DeleteDocument(item)"><i class="fa-solid fa-trash-can"></i></div></td>
                        <td hidden>@item.PaymentItemId</td>
                        <td >@item.CreateDate.ToString("M-dd-yy")</td>
                        <td>@item.Note</td>

                        <td>
                            <table class="table table-striped table-light table-sm ">
                                <thead>
                                    @if(item.Payees.Count > 0)
                                    {
                                        <tr>
                                            <td class="col-6">Payee</td>
                                            <td>Date</td>
                                            <td>Amount</td>
                                        </tr>
                                    }
                                </thead>
                                <tbody>
                                    @foreach(var payee in item.Payees)
                                    {
                                        <tr>
                                            <td>@payee.Name</td>
                                            <td>@payee.Date.ToString("M-dd-yy")</td>
                                            <td>$@payee.AmountFormatted</td>
                                        </tr>
                                    }
                                </tbody>
                            </table>
                        </td>
                    </tr>
                }
        </tbody>

    </table>
</div>


